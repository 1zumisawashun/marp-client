{
  "name": "marp-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx @marp-team/marp-cli -s ./src --theme-set ./src/assets/styles/*.css",
    "build": "marp -I . -o dist && cpx 'assets/**' dist/assets"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.0.4"
  }
}
